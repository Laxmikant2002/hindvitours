export const metadata = {
  title: 'Payment | HindviTours',
  description: 'Secure payment processing for your tour booking',
};

export default function PaymentLayout({ children }) {
  return children;
}

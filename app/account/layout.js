export const metadata = {
  title: 'Account | HindviTours',
  description: 'Manage your HindviTours account settings and preferences',
};

export default function AccountLayout({ children }) {
  return children;
}
